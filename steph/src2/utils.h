#ifndef ST_HEURISTIC_UTILS_H
#define ST_HEURISTIC_UTILS_H

#define ADDR(x) (&(x))

#define MIN(x, y) ((x) <= (y) ? (x) : (y))

#define MAX(x, y) ((x) >= (y) ? (x) : (y))

#endif /* ST_HEURISTIC_UTILS_H */
